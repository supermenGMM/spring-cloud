spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8012/
      profile: default
      label: master
      fail-fast: true
  profiles:
    active: default
server:
  port: 8086